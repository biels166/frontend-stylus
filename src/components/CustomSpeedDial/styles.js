import styled from "styled-components";
import FileDownloadIcon from '@mui/icons-material/FileDownload';
import PictureAsPdfIcon from '@mui/icons-material/PictureAsPdf';
import CancelIcon from '@mui/icons-material/Cancel';
import DoneOutlineIcon from '@mui/icons-material/DoneOutline';
import EditIcon from '@mui/icons-material/Edit';
import VisibilityIcon from '@mui/icons-material/Visibility';
import DeleteForeverIcon from '@mui/icons-material/DeleteForever';

export const CustomViewIcon = styled(VisibilityIcon)({
    color: '#FFFFFF'

})

export const CustomDownloadIcon = styled(FileDownloadIcon)({
    color: '#FFFFFF'
})

export const CustomPDFIcon = styled(PictureAsPdfIcon)({
    color: '#FFFFFF'
})

export const CustomApproveIcon = styled(DoneOutlineIcon)({
    color: '#FFFFFF'
})

export const CustomCancelIcon = styled(CancelIcon)({
    color: '#FFFFFF'
})

export const CustomDeleteDraftIcon = styled(DeleteForeverIcon)({
    color: '#FFFFFF'
})

export const CustomEditDraftIcon = styled(EditIcon)({
    color: '#FFFFFF'
})